// Copie este arquivo para `src/config.js` e preencha com as chaves reais.
// `src/config.js` está no .gitignore — nunca é commitado.
//
// Alternativa: deixar tudo vazio aqui e preencher pela tela de Configurações
// dentro do app (as chaves ficam salvas no localStorage do aparelho).
export const CONFIG = {
  GEMINI_API_KEY: '', // https://aistudio.google.com/apikey
  GOOGLE_CLIENT_ID: '', // Google Cloud Console → Credenciais → ID do cliente OAuth
  DRIVE_FOLDER_ID: '', // ID da pasta CDJ no Google Drive
}
