// Copie este arquivo para `src/config.js` e preencha — OU deixe vazio e use a
// tela de Configurações do app (as chaves ficam no localStorage do aparelho).
// `src/config.js` está no .gitignore — nunca é commitado.
//
// No modelo Apps Script, o app só precisa destes dois valores. Os IDs das pastas
// do Drive e a GEMINI_API_KEY ficam nas Propriedades do Script (lado do Apps
// Script), nunca no navegador. Veja appscript/Codigo.gs para o passo a passo.
export const CONFIG = {
  APPSCRIPT_URL: '', // URL do Web App do Apps Script (termina em /exec)
  APP_TOKEN: '', // token compartilhado, igual ao TOKEN das Propriedades do Script
}
